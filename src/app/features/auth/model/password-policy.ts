/** Правила пароля при регистрации и смене пароля (как на экране `/auth`). */
export const REGISTRATION_PASSWORD_PATTERN =
  /^(?=.*[a-z])(?=.*[A-Z])(?=.*[^a-zA-Z0-9]).{8,}$/;

export const PASSWORD_HINT_TEXT =
  'Пароль должен содержать не менее 8 символов, включающих буквы верхнего и нижнего регистра и специальные символы';

/** Подсказка на экране восстановления пароля (макет). */
export const PASSWORD_HINT_RECOVERY =
  'Пароль должен содержать не менее 8 символов, включающих буквы верхнего и нижнего регистра и специальные символы';

/** Сообщение о несовпадении паролей — восстановление доступа. */
export const PASSWORD_MISMATCH_RECOVERY = 'Набранные вами пароли не совпадают';
